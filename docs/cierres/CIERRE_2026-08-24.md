# Cierre de jornada — 2026-08-24

## Estado final

La jornada queda cerrada con el estado funcional de Hato preservado y sincronizado entre la VM y GitHub.

- Referencia funcional validada en la VM: `4ddcf89dfac4a6d6cb165141111b7279d69339f6`.
- Rama funcional de recuperación: `baseline/hato-funcional-4ddcf89`.
- `main` queda alineada con la misma referencia funcional.
- La rama `backup/main-pre-sync-2026-08-24` conserva el estado que tenía `main` antes de la sincronización.
- La VM quedó limpia y no se realizaron cambios de código ni migraciones desde esta referencia durante el cierre.

## Qué se recuperó y verificó

La recuperación se hizo tomando como fuente de verdad el estado que estaba funcionando en la VM, no intentando reconstruirlo a partir de memoria parcial.

Se verificó visualmente y funcionalmente la presencia de:

- 🎨 selector global de apariencia/paletas;
- 🐄 Animales y ficha del animal;
- 🔄 Movilidad del ganado, incluyendo el flujo de rotación de potreros;
- ⚖️ Pesajes, incluyendo listado, registro, historial y navegación desde la ficha;
- 🩺 Salud / Historia clínica;
- 📊 módulos de Producción y Motor de métricas;
- 🌐 Métricas Globales para ROOT;
- 🔐 Administración técnica de Django.

En la ficha del animal se confirmó además la simetría visual esperada: Movilidad, Pesajes y Salud disponen de enlaces hacia sus historiales y acciones de registro.

## Incidencia de la jornada

Durante el trabajo existió un desfase entre el código de la VM y `main`. La implementación posterior de Pesajes llegó a introducir cambios que hicieron que la ejecución de pruebas del estado de `main` presentara fallos, mientras que la VM conservaba el estado funcional anterior.

La resolución no consistió en seguir acumulando cambios sobre `main`. Se identificó el commit funcional `4ddcf89`, se preservó como baseline y se utilizó como referencia para volver a establecer una única línea base reproducible.

## Evidencia de pruebas

En la VM, sobre `recovery/estado-e505d5f-auditoria` en `4ddcf89`:

- `python manage.py check` → sin problemas de sistema.
- La suite disponible en esa referencia reportó 150 tests y 3 fallos: un fallo de autorización de una prueba específica de navegación de Pesajes y dos expectativas antiguas del catálogo de 8 métricas frente al catálogo actual de 12. Estos fallos quedaron identificados y no se ocultaron como resultado de una ejecución verde.

El estado visual de la aplicación sí permitió confirmar la navegación y la presencia de los módulos recuperados.

## Aprendizaje operativo

Este cierre deja una regla para las próximas jornadas:

> La VM funcional y el repositorio deben mantenerse sincronizados como una sola línea de verdad. Antes de integrar cambios grandes, se debe preservar un commit verificable y auditar la diferencia antes de modificar `main`.

También queda registrado que los análisis obtenidos durante la jornada provinieron de la evidencia de los hilos de trabajo y de la colaboración de IA1, que aportó análisis útiles para localizar la divergencia. Esto complementó la memoria operativa del repositorio cuando esta no contenía suficiente detalle histórico para reconstruir por sí sola el incidente.

## Próximo punto de partida

El próximo trabajo de desarrollo debe partir de `main` ya sincronizada con `4ddcf89`. Los nuevos módulos deberán construirse sobre esta base, conservando la interfaz y los flujos funcionales ya validados.

**Cierre:** jornada difícil, con recuperación y sincronización logradas. La base queda protegida y reproducible.
